-interface
    type Score = 'A' | 'B' | 'C' | 'F'
    
    interface User{
        name: string
        age: number
        gender?: string
        readonly birthYear: number
        [grade:number]: Score
    }

    let user: User={
        name: 'xx'
        age: 30
        birthYear: 2000
        1: 'A',
        2: 'B',
    }

    interface IsAdult{
        (age:number): boolean
    }

    const a:IsAdult = (age)=>{
        return age>19
    }

- const isHorizontal = direction === "horizontal"
    - if direction is horizontal, return true to isHorizontal